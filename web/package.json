{
  "name": "mxid-web",
  "private": true,
  "scripts": {
    "dev:console": "pnpm --filter @mxid/console dev",
    "dev:portal": "pnpm --filter @mxid/portal dev",
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
